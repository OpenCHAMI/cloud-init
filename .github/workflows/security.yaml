# SPDX-FileCopyrightText: Copyright © 2026 OpenCHAMI a Series of LF Projects, LLC
#
# SPDX-License-Identifier: MIT

name: Security

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: security-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  govulncheck:
    name: govulncheck
    uses: OpenCHAMI/github-actions/.github/workflows/govulncheck.yml@c07065a1a490d5576e5ef210dd0c6627f9358fed # v3.10
    with:
      go-package: ./...

  dependency-review:
    name: dependency-review
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
      pull-requests: write
    uses: OpenCHAMI/github-actions/.github/workflows/dependency-review.yml@c07065a1a490d5576e5ef210dd0c6627f9358fed # v3.10
    with:
      fail-on-severity: high
      comment-summary-in-pr: never

  workflow-lint:
    name: workflow lint
    permissions:
      contents: read
      security-events: write
    uses: OpenCHAMI/github-actions/.github/workflows/lint-workflows.yml@c07065a1a490d5576e5ef210dd0c6627f9358fed # v3.10
